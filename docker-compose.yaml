include:
  - docker-compose-db.yaml
services:
  api:
    build:
      context: .
      args:
        - NODE_VERSION=$NODE_VERSION
    ports:
      - 1337:1337
    volumes:
      - .:/app
      - /app/node_modules
